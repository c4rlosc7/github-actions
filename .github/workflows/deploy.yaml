name: "Hello world from github-actions"

on:
  push:
    branches: ["main"]

jobs:
  deploy-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "It done"
      - run: echo "Migrations is done"