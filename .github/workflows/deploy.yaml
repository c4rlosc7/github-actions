name: "Deploy App"

on:
  push:
    branches: ["2-exercise"]

jobs:
  setup-nodes:
    runs-on: ubuntu-latest

    steps:
      - name: Test 
        uses: actions/checkout@v4
      - run: echo "Start deplaoy ..."
    
      - name: setup
        uses: actions/setup-node@v4
        with:
          node-version: '18'
