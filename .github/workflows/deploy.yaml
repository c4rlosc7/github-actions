name: "Deploy to server - github-actions [1-practice]"

on:
  push:
    branches: ["1-practice"]

jobs:
  first_jobs:
    runs-on: ubuntulatest
    steps:
      - name: Hello world
      - run: echo "Hello world from run"